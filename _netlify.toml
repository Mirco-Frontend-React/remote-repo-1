[build]
  publish = "build"   # or "dist" depending on your project

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"          # allow all domains (for production, replace * with your host domain)
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"
